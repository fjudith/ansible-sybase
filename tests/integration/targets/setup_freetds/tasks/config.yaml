- name: Show odbc driver user configuration
  become: true
  shell: |
    odbcinst -j
  args:
    executable: /bin/bash

- name: Configure ODBC driver
  become: true
  copy:
    src: odbcinst.ini
    dest: /etc/odbcinst.ini
    mode: 0644

